# Full-feature developer convenience shim.
# pyproject.toml and mat_viewer.capabilities are the only dependency registry.
# Ordinary CPU PNG/PDF/SVG users should install the base package instead.
.[all]
