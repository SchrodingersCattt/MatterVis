dash
plotly
numpy
scipy
gemmi
pymatgen
kaleido
flask-sock
# brotli + gzip for /_dash-update-component responses. The Mesh3d
# vertex base64 (and our 1 MB-per-frame topology figure) compresses
# ~5x; without this every Labels/Axes click ships a megabyte over
# the wire and feels like a 5-10 s lag on consumer connections.
flask-compress
matplotlib
pillow
ase
networkx
# Molecule identification, disorder handling, per-formula-unit stoichiometry,
# and molecule-level packing polyhedra. Pinned to MCK's disorder-aware
# identify_molecules change (skips cross-PART bonds while preserving whole
# major/minor fragments for rendering).
molcrys-kit @ git+https://github.com/SchrodingersCattt/MolCrysKit.git@a1ac70477895bbdfcab695ed92c0ac4840193274
