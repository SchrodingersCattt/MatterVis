dash
plotly
numpy
scipy
gemmi
pymatgen
kaleido
flask-sock
# brotli + gzip for /_dash-update-component responses. The Mesh3d
# vertex base64 (and our 1 MB-per-frame topology figure) compresses
# ~5x; without this every Labels/Axes click ships a megabyte over
# the wire and feels like a 5-10 s lag on consumer connections.
flask-compress
matplotlib
pillow
ase
networkx
# Molecule identification, disorder handling, per-formula-unit stoichiometry,
# and molecule-level packing polyhedra. Pinned to the merge of MCK PR #32
# (find_polyhedra(level='molecule') with split cutoff/hard_cutoff
# semantics + auto-bumped molecule search radius) on main.
molcrys-kit @ git+https://github.com/SchrodingersCattt/MolCrysKit.git@9e8884d
