dash
plotly
numpy
scipy
gemmi
pymatgen
kaleido
flask-sock
# brotli + gzip for /_dash-update-component responses. The Mesh3d
# vertex base64 (and our 1 MB-per-frame topology figure) compresses
# ~5x; without this every Labels/Axes click ships a megabyte over
# the wire and feels like a 5-10 s lag on consumer connections.
flask-compress
matplotlib
pillow
ase
networkx
textual
# Molecule identification, disorder handling, per-formula-unit stoichiometry,
# and molecule-level packing polyhedra. Pinned to MCK 0.4.0 (cluster carver,
# disorder-aware identify_molecules, decoupled disorder symmetry copies,
# and the gap+enclosure vs hard_cutoff radial knob split that MV's polyhedron
# editor exposes per spec).
molcrys-kit @ git+https://github.com/SchrodingersCattt/MolCrysKit.git@62fc7e78095634819fad54a29c459a7dad285833
